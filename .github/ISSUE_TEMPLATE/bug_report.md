---
name: Bug report
about: Something in the TypeScript or Python runtime doesn't behave as documented
title: ""
labels: bug
assignees: ""
---

**Runtime affected**
`typescript/` / `python/` / both / `specs/` / `docs/`

**What happened**
A clear description of the actual behavior, including any error output.

**What you expected**
What the docs (`DESIGN.md`, `reference/`, or the docs site) led you to expect instead.

**Steps to reproduce**
```
# exact commands, from a clean clone
```

**Environment**
- OS:
- Node version (if TypeScript):
- Python version (if Python, `uv run python --version`):
- Provider in use: mock / anthropic / openai / google

**Additional context**
Trace output, if relevant (see `/trace` in `.claude/commands/` for a readable rendering).
